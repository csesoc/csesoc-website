services:
  frontend:
    build: ./frontend
    container_name: csesoc-website-frontend
    ports:
      - "3000:3000"
    restart: always
    networks:
      - csesoc-website-network

networks:
  csesoc-website-network:
    driver: bridge
